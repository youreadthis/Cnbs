

# Класс Region

Инструмент для заполнения произвольного многоугольника окружностями ("вышками") трех разных размеров. Алгоритм старается расставить их максимально плотно, не допуская сильного наложения друг на друга и появления дыр.

## Что нужно для работы

**1. Координаты полигона**
Список кортежей `(x, y)`, описывающих границы региона.
```python
coords = [(0, 0), (300, 0), (300, 200), (150, 300), (0, 200)]
```

**2. Радиусы окружностей**
Три числа (`float` или `int`). Важно: R1 должен быть самым большим, R3 — самым маленьким.
```python
R1, R2, R3 = 40, 20, 4.5
```

**3. Проценты "чистой" площади**
Числа от 0 до 100 (`int`). Параметр определяет, какой процент площади круга **обязан** быть свободным от пересечений с другими кругами.
*   `80` — строго (мало пересечений).
*   `20` — плотно (сильные наложения).
```python
percent1, percent2, percent3 = 80, 65, 90
```

---

## Как использовать

Есть два способа запуска.

### Способ 1: Всё сразу (при создании)
Передаем координаты, радиусы и проценты сразу в конструктор. Расчет произойдет автоматически.

```python
# Создаем и сразу считаем
region = Region(
    coordinates=coords, 
    r1=R1, r2=R2, r3=R3, 
    percent1=80, 
    percent2=70, 
    percent3=60
)

# Выводим красивый отчет
print(region)
```

### Способ 2: Пошагово
Сначала создаем регион, а расчет запускаем, когда нужно.

```python
# 1. Просто создаем регион
region = Region(coords)

# ... какая-то другая логика ...

# 2. Запускаем расчет
towers = region.find_all_centers_of_towers(
    r1=40, r2=20, r3=5, 
    percent1=80, percent2=80, percent3=80
)

print(region)
```

---

## Что получаем на выходе

### Отчет в консоль (`print(region)`)
Выведет площадь региона, общее количество вышек и их разбивку по типам:
```text
========================================
📍 ОТЧЕТ О РЕГИОНЕ
========================================
...
Инфраструктура:
  • Всего вышек: 158
  • Детализация:
  • R1: 28 шт.
  • R2: 45 шт.
  • R3: 85 шт.
========================================
```

### Координаты центров
Можно получить словарь со списками координат `(x, y)` для каждого радиуса:

```python
data = region.get_centers_of_towers()

# Доступ к спискам:
list_r1 = data['r1_centers']
list_r2 = data['r2_centers']
list_r3 = data['r3_centers']
```